storage:
  trace:
    backend: s3
    s3:
      tls_insecure_skip_verify: true
      bucket: tempo                        # store traces in this bucket
      endpoint: default-hl.minio.svc.cluster.local:9000  # api endpoint
      access_key: Als13cgkzAkBZcxG0ZWC                                 # optional. access key when using static credentials.
      secret_key: cQEYqadNi6u5WkLgUBm34mzNB0158ZLV6BPK040i                           # optional. secret key when using static credentials.
      insecure: true

metricsGenerator:
  # -- Specifies whether a metrics-generator should be deployed
  affinity: {}
  replicas: 1
ingester:
  affinity: {}
distributor:
  affinity: {}